# =============================================================================
# Analytics LLM - Hugging Face Spaces Requirements
# CPU-only build — pinned stable versions to avoid torch/nn import bugs
# torch is installed separately in Dockerfile BEFORE this file
# =============================================================================

# -----------------------------------------------------------------------------
# Core Flask & Server
# -----------------------------------------------------------------------------
flask==3.0.0
flask-cors==4.0.0
flask-socketio>=5.3.0
flask-sock>=0.7.0
simple-websocket>=1.0.0
eventlet>=0.33.0

# -----------------------------------------------------------------------------
# Database
# -----------------------------------------------------------------------------
pymongo==4.6.0

# -----------------------------------------------------------------------------
# Config & Utilities
# -----------------------------------------------------------------------------
python-dotenv==1.0.0
requests==2.31.0
aiofiles>=23.0.0

# -----------------------------------------------------------------------------
# SPEECH-TO-TEXT (STT) — CPU mode
# torch installed via Dockerfile first (CPU whl), these depend on it
# -----------------------------------------------------------------------------
faster-whisper>=1.0.0
ctranslate2>=4.0.0

# HuggingFace model hub (needed for whisper model download)
huggingface-hub>=0.20.0

# PINNED: transformers 4.40.2 is last stable before the nn.Module type-annotation bug
# DO NOT upgrade above 4.40.x — causes NameError: name 'nn' is not defined
transformers==4.40.2

# PINNED: must match transformers 4.40.x
accelerate==0.26.0

# -----------------------------------------------------------------------------
# Audio processing
# -----------------------------------------------------------------------------
pydub>=0.25.0
# av omitted — ffmpeg binary (installed via apt) handles decoding

# -----------------------------------------------------------------------------
# TEXT-TO-SPEECH (TTS)
# -----------------------------------------------------------------------------
elevenlabs>=1.0.0
edge-tts>=6.1.9

# -----------------------------------------------------------------------------
# RAG — Vector store + Embeddings
# PINNED: sentence-transformers 2.7.0 tested with transformers 4.40.x
# -----------------------------------------------------------------------------
chromadb==0.4.22
sentence-transformers==2.7.0

# LangChain (core only)
langchain>=0.1.6
langchain-community>=0.0.20

# -----------------------------------------------------------------------------
# LLM API (OpenRouter = OpenAI-compatible)
# -----------------------------------------------------------------------------
openai>=1.0.0

# -----------------------------------------------------------------------------
# Numpy — keep below 2.0 for compatibility
# -----------------------------------------------------------------------------
numpy>=1.24.0,<2.0.0
